import React, { Fragment } from "react";
import HomeContent from "../components/home-content";

function Home(props) {
  return (
    <Fragment>
      <HomeContent books={[]} />
    </Fragment>
  );
}

export default Home;
